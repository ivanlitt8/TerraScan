import PlaceholderScreen from "@/components/PlaceholderScreen";
import { FileText } from "lucide-react";

export default function ReportesPage() {
  return (
    <PlaceholderScreen
      title="Reportes"
      description="La exportación avanzada de datos y reportes de tus lotes estará disponible pronto."
      icon={FileText}
    />
  );
}
